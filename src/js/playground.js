require([
    '$',
    'pinny',
    'sheet-right'
],
function(
    $,
    Pinny,
    sheetRight
) {
    var _playgroundUI = function() {
        console.log('Playground UI');
    };

    _playgroundUI();
}, undefined, true);
// relPath, forceSync
